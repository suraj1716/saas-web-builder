// src/templates/index.ts
import { WebsiteInstance } from "@/web-builder/types/types";
import { SimpleWebsite } from "./SimpleWebsite/SimpleWebsite";

 const templates: WebsiteInstance[] = [
  SimpleWebsite
  // add more templates here later
];

export default templates